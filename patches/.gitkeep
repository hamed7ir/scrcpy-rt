This directory is deliberately empty.

Git cannot track an empty directory, so without this file `patches/` would not exist in a
clone -- and the README, THIRD-PARTY-NOTICES.md and UPSTREAM.md all invite you to verify the
zero patch count by looking at it. An absent directory is not the same evidence as an empty one.

Patches applied by this repository to upstream: NONE.
